services:
  - type: web
    name: floorplan-pro
    env: docker
    plan: starter
    region: oregon
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
